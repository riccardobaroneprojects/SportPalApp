# AuthHandler
this authentication handler is used for UI state changes (i.e. shows sign in modal)

# GeoJsonHandler

thi converts locations data from search feature to GeoJSON file for mapbox to render pins on the map